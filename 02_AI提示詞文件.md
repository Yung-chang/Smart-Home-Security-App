# 智慧家庭資安 APP — 完整 AI 提示詞文件
**SmartHome Guardian — AI Prompts for Cursor / GitHub Copilot / ChatGPT**

> 使用方式：將各段提示詞貼入 Cursor/ChatGPT 或作為 GitHub Copilot Chat 指令。
> 建議依序執行，確保上下文一致。

---

## 📋 MASTER PROMPT（主提示詞）

```
你是一位資深 Android 工程師，專精資訊安全與智慧家庭系統開發。
請為以下專案生成完整、可正式使用的程式碼：

【專案名稱】SmartHome Guardian
【目標平台】Android (minSdk 26 / targetSdk 35)
【主要語言】Kotlin
【UI 框架】Jetpack Compose + Material Design 3
【架構模式】MVVM + Clean Architecture（三層：Presentation / Domain / Data）
【相依注入】Hilt
【非同步】Kotlin Coroutines + Flow

【核心功能】
1. 生物辨識 + PIN 雙因素認證
2. 家庭設備儀表板（燈光/門鎖/攝影機/感應器）
3. 即時安全監控（WebSocket + MQTT）
4. 角色存取控制（管理員/家庭成員/訪客）
5. 稽核日誌（不可竄改，HMAC 簽章）

【資安要求】
- 所有儲存使用 AES-256-GCM 加密
- 所有網路通訊 TLS 1.3 + Certificate Pinning
- Android Keystore 管理加密金鑰
- Root 偵測與模擬器偵測
- FLAG_SECURE 防截圖

請嚴格遵守 OWASP Mobile Top 10 安全準則。
程式碼需包含完整的 KDoc 文件註解、錯誤處理、及 Unit Test。
```

---

## 🏗️ PROMPT 01：專案初始化與 Gradle 設定

```
基於上述 SmartHome Guardian 專案，請生成以下 Gradle 設定：

1. 【app/build.gradle.kts】完整設定，包含：
   - compileSdk 35, minSdk 26, targetSdk 35
   - buildFeatures: compose, buildConfig
   - composeOptions: kotlinCompilerExtensionVersion 最新穩定版
   - 所有必要 dependencies：
     * Jetpack Compose BOM (最新穩定)
     * Hilt (Dagger Hilt 2.50+)
     * Retrofit + OkHttp (4.12+)
     * Room + SQLCipher (加密資料庫)
     * EncryptedSharedPreferences (Security Crypto)
     * Eclipse Paho MQTT Android
     * Firebase BOM (FCM)
     * Coil (圖片載入)
     * BiometricPrompt
     * RootBeer (Root 偵測)
     * Timber (日誌)
     * Mockk + JUnit5 (測試)
   - proguardFiles 設定
   - signingConfigs 結構

2. 【gradle/libs.versions.toml】版本目錄

3. 【build.gradle.kts（根目錄）】Hilt classpath 設定

確保所有版本互相相容，不要使用 alpha/beta 版本。
```

---

## 🔐 PROMPT 02：安全核心模組

```
為 SmartHome Guardian 實作資安核心模組：

【檔案清單】
1. security/CryptoManager.kt
   - 使用 Android Keystore 產生/管理 AES-256-GCM 金鑰
   - 函數：encrypt(plaintext: ByteArray): EncryptedData
   - 函數：decrypt(encryptedData: EncryptedData): ByteArray
   - 函數：generateKeyPair(alias: String): KeyPair（用於設備認證）
   - 金鑰不導出（setUserAuthenticationRequired 可選）

2. security/TokenManager.kt
   - 使用 EncryptedSharedPreferences 儲存 JWT access token + refresh token
   - 函數：saveTokens(access: String, refresh: String)
   - 函數：getAccessToken(): String?
   - 函數：isTokenExpired(): Boolean（解析 JWT exp claim）
   - 函數：clearTokens()
   - Token 自動更新邏輯（OkHttp Interceptor）

3. security/SecurityChecker.kt
   - 使用 RootBeer 偵測 Root
   - 偵測模擬器（Build.FINGERPRINT / MANUFACTURER 判斷）
   - 偵測 USB 除錯模式
   - 偵測 ADB 連線
   - 函數：runSecurityChecks(): SecurityCheckResult（列出所有問題）
   - 允許開發模式繞過（BuildConfig.DEBUG）

4. security/CertificatePinner.kt
   - OkHttpClient 的 CertificatePinner 設定
   - 支援多個 domain 的 pin 設定
   - 包含 backup pin 機制

5. security/HmacSigner.kt
   - 使用 HMAC-SHA256 對操作紀錄/設備指令簽章
   - 函數：sign(data: String, key: ByteArray): String
   - 函數：verify(data: String, signature: String, key: ByteArray): Boolean

每個類別需包含：
- 完整 KDoc 文件
- 詳細錯誤處理（自訂 Exception 類別）
- 對應的 Unit Test（使用 JUnit5 + Mockk）
```

---

## 🔑 PROMPT 03：身份驗證功能

```
為 SmartHome Guardian 實作完整的身份驗證功能：

【AuthState 密封類別】
sealed class AuthState {
    object Idle, Loading, RequiresBiometric, RequiresPin,
    Authenticated(val user: User), 
    Error(val message: String, val code: AuthErrorCode)
}

【AuthViewModel.kt】
- StateFlow<AuthState> 驅動 UI
- 函數：loginWithCredentials(email, password)
- 函數：loginWithBiometric(activity: FragmentActivity)
- 函數：verifyPin(pin: String)
- 函數：logout()
- 函數：refreshSession()
- 使用 Hilt @HiltViewModel

【BiometricHelper.kt】
- 封裝 BiometricPrompt API
- 支援 CLASS_3（強生物辨識）
- 降級到 PIN/密碼
- 函數：authenticate(activity, onSuccess, onFailure, onError)
- 函數：isBiometricAvailable(context): BiometricAvailability

【LoginScreen.kt（Compose）】
設計需求：
- 深色主題，科技感設計
- SmartHome Guardian Logo（矢量圖示）
- Email + Password 輸入欄（密碼欄有顯示/隱藏切換）
- 「使用生物辨識登入」按鈕（指紋圖示）
- 「使用 PIN 碼登入」按鈕
- Loading 狀態（CircularProgressIndicator）
- 錯誤訊息顯示（紅色 Snackbar）
- 輸入欄位鍵盤為 ImeAction.Next / Done
- 防止截圖（LaunchedEffect 設定 FLAG_SECURE）

【BiometricScreen.kt（Compose）】
- 全螢幕生物辨識提示頁
- 動態指紋動畫（Lottie 或 Canvas 繪製）
- 「使用 PIN 備援」文字連結

包含完整的 Compose Preview，並使用 Hilt 注入 ViewModel。
```

---

## 🏠 PROMPT 04：主儀表板

```
為 SmartHome Guardian 實作主儀表板（Dashboard）：

【DashboardScreen.kt（Compose）】
整體佈局：
- 頂部：AppBar（用戶頭像、通知鈴鐺 badge、設定）
- 安全狀態橫幅：全螢幕寬，顯示當前安全等級（綠色 SECURE / 黃色 WARNING / 紅色 ALERT）
- 房間選擇器：水平可捲動 ChipGroup（客廳/臥室/廚房/門口/全部）
- 設備卡片格：LazyVerticalGrid（2 欄），每張卡包含：
  * 設備圖示（根據 DeviceType 顯示不同 Icon）
  * 設備名稱
  * 狀態指示燈（綠色 ONLINE / 灰色 OFFLINE / 紅色 ERROR）
  * 快速開關 Toggle（僅允許操作的設備）
  * 最後活動時間
- 底部：最近警報列表（最新 3 筆）
- FAB：新增設備

【DeviceCard.kt（Compose 元件）】
- 使用 AnimatedVisibility 做狀態切換動畫
- 長按顯示快速操作選單（鎖定/解鎖/查看詳情/查看日誌）
- 設備離線時卡片顯示灰階效果
- 使用 Material 3 ElevatedCard

【SecurityStatusBanner.kt（Compose 元件）】
- 根據 SecurityLevel 動態變色
- 顯示：「所有系統正常」/「1 個警報待處理」/「系統遭入侵偵測！」
- 點擊跳至安全監控頁

【DashboardViewModel.kt】
- StateFlow<DashboardUiState> 包含：設備列表、安全等級、未讀警報數
- 函數：toggleDevice(deviceId: String, newState: Boolean)
- 函數：refreshDevices()
- 訂閱 WebSocket 設備狀態更新
- 訂閱安全警報 Flow

所有 UI 需支援 Dark Theme，使用 Material 3 色彩系統。
```

---

## 📱 PROMPT 05：設備管理功能

```
為 SmartHome Guardian 實作設備管理功能：

【DeviceDetailScreen.kt（Compose）】
根據設備類型顯示不同控制面板：

DOOR_LOCK（智慧門鎖）：
- 大型鎖頭動畫（鎖定/解鎖狀態切換）
- 開鎖/上鎖按鈕（需確認對話框）
- 進出記錄時間軸（Timeline）
- 臨時密碼生成功能
- 電池電量顯示

CAMERA（監控攝影機）：
- RTSP 串流預覽框（使用 ExoPlayer）
- 截圖按鈕（儲存至加密相簿）
- 偵測靈敏度調整（Slider）
- 移動偵測區域設定（Canvas 可繪製遮罩）
- 錄影記錄列表

LIGHT（智慧燈光）：
- 亮度 Slider（0-100%）
- 色溫調整（Slider）
- 色彩選取器（圓形 ColorPicker）
- 場景預設（閱讀/電影/睡眠/工作）
- 定時開關設定

SENSOR（感應器）：
- 當前感應值顯示（門窗開合/溫濕度/煙霧/CO）
- 歷史數據折線圖（Vico 圖表）
- 警報閾值設定

【DeviceRepository.kt + Impl】
- 函數：getDevices(): Flow<List<Device>>
- 函數：getDevice(id: String): Flow<Device?>
- 函數：sendCommand(deviceId: String, command: DeviceCommand): Result<Unit>
- 函數：updateSettings(device: Device): Result<Unit>
- 本地快取 + 遠端同步策略
- 使用 HMAC 對每個 DeviceCommand 簽章

所有設備控制操作需：
1. 先驗證用戶有操作權限（AccessRule 檢查）
2. 記錄到稽核日誌
3. 顯示操作確認對話框（重要操作）
```

---

## 🛡️ PROMPT 06：安全監控功能

```
為 SmartHome Guardian 實作安全監控模組：

【SecurityMonitorScreen.kt（Compose）】
佈局：
- 頂部：安全評分圓形進度條（0-100，動態顏色）
- 威脅等級地圖：家平面圖（Canvas 繪製），各房間根據警報狀態標色
- 即時事件串流：LazyColumn，新事件從頂部插入（動畫）
- 每個事件項目：嚴重程度 Badge + 設備名稱 + 時間 + 描述

【AlertHistoryScreen.kt（Compose）】
- 篩選器：日期範圍選擇 / 嚴重程度 / 設備 / 類型
- 時間軸列表（按日期分組）
- 每筆警報展開顯示詳細資訊
- 批次確認（ACK）選取功能
- 匯出 PDF/CSV 按鈕

【SecurityViewModel.kt】
- StateFlow<SecurityUiState>
- 訂閱 WebSocket SecurityAlert 串流
- 函數：acknowledgeAlert(alertId: String)
- 函數：bulkAcknowledge(alertIds: List<String>)
- 函數：getAlertHistory(filter: AlertFilter): Flow<List<SecurityAlert>>
- 本地通知發送（NotificationManager）：CRITICAL 警報立即彈出

【AlertEngine.kt（本地規則引擎）】
即使 APP 在背景，仍可執行的規則：
- 規則 1：設備離線超過 X 分鐘
- 規則 2：短時間內同一設備多次操作失敗
- 規則 3：非授權時間偵測到門鎖操作
- 規則 4：Token 刷新失敗超過 3 次（可能遭竊）
- 每條規則觸發後：寫入 SecurityAlert + 發送 FCM + 更新本地通知

【SecurityService.kt（Foreground Service）】
- 保持 MQTT 連線在背景運行
- 顯示持續通知（「SmartHome Guardian 保護中」）
- 接收 MQTT 設備事件並轉發給 AlertEngine
```

---

## 👥 PROMPT 07：存取控制管理

```
為 SmartHome Guardian 實作存取控制管理功能：

【AccessControlScreen.kt（Compose）】
分頁設計（TabRow）：
- Tab 1「用戶管理」：
  * 用戶列表（頭像 + 名稱 + 角色 Badge + 最後登入）
  * 新增用戶 FAB（邀請 Email + 選擇角色）
  * 點擊用戶 → UserDetailSheet（BottomSheet）

- Tab 2「存取規則」：
  * 規則列表（設備 → 用戶 → 時間窗口 → 操作）
  * 規則新增精靈（Step 1: 選設備, Step 2: 選用戶, Step 3: 設時間, Step 4: 選操作）
  * 規則啟用/停用 Toggle
  * 衝突偵測警告（兩條規則互相矛盾時標紅）

- Tab 3「臨時存取」：
  * 訪客 QR Code 生成（含到期時間 + 允許設備清單）
  * 已生成臨時存取碼列表 + 撤銷按鈕

【UserManagementViewModel.kt】
- 函數：getUsers(): Flow<List<User>>
- 函數：inviteUser(email: String, role: UserRole): Result<Unit>
- 函數：updateUserRole(userId: String, role: UserRole): Result<Unit>
- 函數：revokeUserAccess(userId: String): Result<Unit>
- 函數：generateGuestQrCode(devices: List<String>, expiry: Long): Result<QrCodeData>

【AccessRule 資料模型】
data class AccessRule(
    val id: String,
    val userId: String,          // 或 "*" 表示所有人
    val deviceId: String,        // 或 "*" 表示所有設備
    val allowedOperations: Set<Operation>,
    val timeWindow: TimeWindow?, // null = 不限時間
    val isEnabled: Boolean,
    val expiresAt: Long?         // null = 永不過期
)

data class TimeWindow(
    val daysOfWeek: Set<DayOfWeek>,
    val startTime: LocalTime,
    val endTime: LocalTime,
    val timezone: ZoneId
)

存取控制邏輯（AccessController.kt）：
- 函數：canUserOperate(userId, deviceId, operation): Boolean
- 規則衝突時：拒絕優先（Deny overrides Allow）
- 所有決策寫入稽核日誌
```

---

## 📜 PROMPT 08：稽核日誌功能

```
為 SmartHome Guardian 實作防竄改稽核日誌功能：

【AuditLogScreen.kt（Compose）】
- 搜尋欄（可搜尋用戶/設備/操作類型）
- 時間範圍篩選器（DateRangePicker）
- 日誌列表（LazyColumn，按日期分組）
- 每筆日誌：
  * 操作類型 Icon + 顏色（登入/控制設備/修改設定/刪除等）
  * 用戶名稱 + 操作描述
  * 設備/目標 + 時間戳
  * HMAC 簽章狀態 Badge（✓ 有效 / ✗ 已竄改 / ? 無法驗證）
  * 展開顯示：操作前/後狀態（JSON diff 視覺化）
- 匯出按鈕：CSV / PDF（加密保護）
- 篩選：只顯示異常/疑似竄改日誌

【AuditRepository.kt + Impl】
- 函數：getLogs(filter: AuditFilter): Flow<PagingData<AuditLog>>（Paging 3）
- 函數：writeLog(log: AuditLog): Result<Unit>（自動計算 HMAC）
- 函數：verifyLogIntegrity(logId: String): IntegrityResult
- 函數：exportLogs(filter: AuditFilter, format: ExportFormat): Result<Uri>

【AuditLogger.kt（單例）】
- 由 Hilt 提供，全 APP 注入
- 函數：log(action: AuditAction, targetId: String?, before: Any?, after: Any?)
- 自動填充：userId, timestamp, ipAddress, deviceFingerprint
- 自動計算 HMAC（使用 HmacSigner）
- 批次寫入（避免頻繁 DB 寫入，每秒批次插入）
- Room DAO 使用 @Transaction 確保原子性

【稽核動作清單（AuditAction enum）】
LOGIN_SUCCESS, LOGIN_FAILED, LOGOUT,
DEVICE_CONTROL, DEVICE_SETTINGS_CHANGED,
ACCESS_RULE_CREATED, ACCESS_RULE_DELETED,
USER_INVITED, USER_ROLE_CHANGED, USER_REVOKED,
ALERT_ACKNOWLEDGED, SYSTEM_SETTING_CHANGED,
EXPORT_LOGS, TOKEN_REFRESH, AUTH_BYPASS_ATTEMPT
```

---

## 🌐 PROMPT 09：網路層與 MQTT

```
為 SmartHome Guardian 實作完整網路通訊層：

【NetworkModule.kt（Hilt Module）】
提供以下 singleton：
1. OkHttpClient（含 CertificatePinner, AuthInterceptor, LoggingInterceptor）
2. Retrofit（設定 GsonConverterFactory, 基礎 URL）
3. WebSocketClient（OkHttp WebSocket）

【AuthInterceptor.kt】
- 所有 API 請求自動附加 Bearer Token
- 收到 401 時自動 refresh token 後重試
- Refresh 失敗時發出 logout 事件（SharedFlow）
- 使用 Mutex 防止多個請求同時 refresh

【WebSocketManager.kt】
- 管理 WSS 連線生命週期
- 自動重連（指數退避策略：1s, 2s, 4s, 8s... 最大 60s）
- 函數：connect(token: String)
- 函數：disconnect()
- 函數：observeDeviceStatus(deviceId: String): Flow<DeviceStatus>
- 函數：observeAlerts(): Flow<SecurityAlert>
- 函數：observeSystemEvents(): Flow<SystemEvent>
- 心跳機制（每 30 秒 ping）

【MqttManager.kt】
- Eclipse Paho Android 封裝
- TLS 連線（port 8883）
- 使用 Android Keystore 的 Client Certificate 認證
- 函數：connect(broker: String, clientId: String)
- 函數：subscribe(topic: String): Flow<MqttMessage>
- 函數：publish(topic: String, payload: ByteArray, qos: Int)
- QoS 2 確保重要指令不遺失
- 自動重連（與 WebSocket 相同策略）

【LocalNetworkDiscovery.kt】
- 使用 mDNS/NSD 自動發現區域網路內的 Home Gateway
- 函數：discoverGateways(): Flow<List<GatewayDevice>>
- 支援 HomeKit / Matter 協定探索

所有網路層需包含：
- 完整 error handling（網路超時/無連線/伺服器錯誤）
- 離線時的本地快取策略
- Timber 日誌（生產模式不記錄敏感資訊）
```

---

## 🎨 PROMPT 10：UI 主題與設計系統

```
為 SmartHome Guardian 建立完整 UI 設計系統：

【Color.kt】設計主題（深色科技感）：
Primary: #00D4FF（青藍色，科技感）
Secondary: #7B2FBE（紫色，資安感）
Background: #0A0E1A（深海軍藍）
Surface: #121827
Error: #FF4444
Success: #00C853
Warning: #FFB300
OnPrimary: #000000
Text Primary: #FFFFFF
Text Secondary: #8899AA

安全等級色：
SECURE: #00C853（綠色）
WARNING: #FFB300（琥珀色）
CRITICAL: #FF4444（紅色）

【Typography.kt】
- 使用 Roboto + Roboto Mono（數值/代碼顯示）

【Shape.kt】
- 卡片：12dp 圓角
- 按鈕：8dp 圓角
- 底部表單：頂部 24dp 圓角

【Theme.kt】
- 僅深色主題（安全 APP 不建議淺色）
- 動態顏色（Android 12+ Material You）停用（避免敏感 UI 被主題影響）

【共用元件】
SecurityBadge.kt：嚴重程度 Badge（CRITICAL/HIGH/MEDIUM/LOW）
StatusIndicator.kt：設備狀態燈（圓點 + 脈衝動畫，ONLINE 時）
EncryptedText.kt：顯示被遮蔽的敏感資訊（點擊後短暫顯示）
ConfirmDialog.kt：重要操作確認對話框（帶操作描述 + 圖示）
TimelineItem.kt：時間軸列表項目
BiometricButton.kt：生物辨識按鈕（帶狀態動畫）
SignalStrengthIcon.kt：設備訊號強度圖示
```

---

## 🧪 PROMPT 11：測試套件

```
為 SmartHome Guardian 實作完整測試：

【單元測試】
1. CryptoManagerTest.kt
   - 測試加密/解密循環（encrypt → decrypt = 原文）
   - 測試不同輸入大小（1B, 1KB, 1MB）
   - 測試金鑰不存在時的錯誤處理

2. AuthViewModelTest.kt
   - 使用 Mockk mock Repository
   - 測試登入成功狀態轉換
   - 測試登入失敗錯誤訊息
   - 測試 Token 過期自動更新

3. AccessControllerTest.kt
   - 測試各角色權限邊界
   - 測試時間窗口規則（正確時間 / 超出時間）
   - 測試規則衝突解決

4. AuditLoggerTest.kt
   - 測試 HMAC 簽章正確性
   - 測試日誌完整性驗證
   - 測試批次寫入

【整合測試】
5. DeviceRepositoryTest.kt
   - 使用 Room in-memory DB
   - 測試 CRUD 操作
   - 測試本地快取更新

6. WebSocketManagerTest.kt
   - 使用 MockWebServer
   - 測試重連策略
   - 測試訊息解析

【UI 測試（Compose Testing）】
7. LoginScreenTest.kt
   - 測試空白輸入驗證
   - 測試錯誤訊息顯示
   - 測試生物辨識按鈕可見性

8. DashboardScreenTest.kt
   - 測試設備列表渲染
   - 測試 Toggle 互動
   - 測試安全狀態橫幅顯示

所有測試使用 JUnit5 + Mockk + Turbine（Flow 測試）+ Coroutines Test。
測試覆蓋率目標：Unit > 80%, Integration > 60%。
```

---

## 🔔 PROMPT 12：推播通知

```
為 SmartHome Guardian 實作完整推播通知系統：

【FCM 設定】
1. FirebaseMessagingService.kt
   - 覆寫 onMessageReceived
   - 根據 data payload 的 alertType 建立不同通知
   - CRITICAL: 使用 Notification.Builder 全螢幕 Intent
   - 解析 payload 並更新本地 SecurityAlert DB
   - 喚醒 AlertEngine 執行本地規則

2. NotificationHelper.kt
   - 建立通知頻道（Android 8+）：
     CHANNEL_CRITICAL（最高優先度，繞過勿擾模式）
     CHANNEL_SECURITY（高優先度）
     CHANNEL_DEVICE（預設優先度）
     CHANNEL_INFO（低優先度）
   - 函數：showAlertNotification(alert: SecurityAlert)
   - 函數：showDeviceOfflineNotification(device: Device)
   - 函數：updateServiceNotification(status: String)（Foreground Service 用）

3. 通知點擊行為：
   - 點擊 CRITICAL 通知 → 開啟 SecurityMonitorScreen，自動捲到該警報
   - 點擊設備離線通知 → 開啟 DeviceDetailScreen
   - 通知帶有「確認」快捷操作按鈕（不需開啟 APP）

確保通知內容不洩露敏感資訊（例如：不顯示完整地址，只顯示「您的門口」）。
Notification 使用 BigTextStyle 顯示詳情。
```

---

## 📝 提示詞使用建議

1. **初次使用**：先執行 MASTER PROMPT，讓 AI 了解全域上下文
2. **逐步建構**：依 PROMPT 01 → 12 的順序執行
3. **保持上下文**：在同一個對話中執行，或每次附上 MASTER PROMPT
4. **迭代調整**：每個 Prompt 執行後，若有不符預期之處，追加要求
5. **程式碼審查**：每個模組完成後，要求 AI 自我審查資安問題

### 常用追加指令
```
「請確保上述程式碼符合 OWASP Mobile Top 10」
「請為這個模組補充完整的 KDoc 文件」
「請生成上述程式碼的 Unit Test」
「請確保沒有硬編碼的密鑰或密碼」
「請加入 Timber 日誌，但不要記錄 token 或密碼」
```
